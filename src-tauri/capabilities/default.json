{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the main window. The webview may call only the enumerated #[tauri::command]s; no generic shell or fs command is exposed (spec 2.8). The one plugin permission is dialog:allow-open — the native folder picker for 'Open Folder…' (Phase 5); no save/message/confirm dialog surface is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
